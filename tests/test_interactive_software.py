# Tests for the removed "interactive" and "software" commands.
# These commands were removed during the CLI refactor.
# The underlying infrastructure (SoftwareService, DriverService) is tested
# in test_catalog_bundle.py and test_detection_engine.py.
